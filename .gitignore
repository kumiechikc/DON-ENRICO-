# Dependencies
node_modules/
package-lock.json
pnpm-lock.yaml
yarn.lock

# Next.js build output
.next/
out/

# Audit / Playwright output (regenerate locally; examples/ is committed on purpose)
audit-output/
test-results/
playwright-report/
.playwright/

# Local Claude Code state
.claude/settings.local.json
.claude/.session/

# Env / secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.coverage
*.egg-info/

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
*.tsbuildinfo
.checks/

# Auditoria de referência: 3 MB de capturas de tela de sites de terceiros, gerados
# uma vez para escrever `docs/BRIEF-MOVIMENTO.md` §2. O achado ficou no documento e o
# método está em §2.0, que basta para refazer. O material bruto não precisa de versão.
.tmp-audit/
